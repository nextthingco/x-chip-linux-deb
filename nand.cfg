CONFIG_MTD=y
CONFIG_MTD_CMDLINE_PARTS=y
# Parse the fixed-partitions (incl. the rootfs `slc-mode;` flag) straight
# from the device tree. Without this only cmdline mtdparts= works, and the
# DT partitions are silently ignored -> the whole chip shows up as one mtd.
CONFIG_MTD_OF_PARTS=y
CONFIG_MTD_RAW_NAND=y
CONFIG_MTD_NAND_SUNXI=y
CONFIG_MTD_UBI=y
CONFIG_MISC_FILESYSTEMS=y
CONFIG_UBIFS_FS=y

# AXP209 PMIC + its i2c bus, BUILT-IN. The USB OTG phy (phy-sun4i-usb) takes the
# PMIC's `usb-power` regulator as a supplier; under armmp these are modules, so in
# the module-less installer initramfs the PMIC never probes -> usb-power never
# registers -> the (built-in) phy defers forever -> no usb0 gadget ("WARN: usb0
# not present"). Building them in satisfies the supplier chain
# (i2c -> axp20x mfd -> regulator -> usb-power).
CONFIG_I2C=y
CONFIG_I2C_MV64XXX=y
CONFIG_MFD_AXP20X=y
CONFIG_MFD_AXP20X_I2C=y
CONFIG_REGULATOR=y
CONFIG_REGULATOR_AXP20X=y

# USB device-controller (peripheral) side of the OTG musb so the NAND
# installer initramfs can bring up a USB-network gadget. The sunxi musb
# glue and phy:
CONFIG_USB_MUSB_HDRC=y
CONFIG_USB_MUSB_SUNXI=y
CONFIG_USB_MUSB_GADGET=y
CONFIG_USB_SUNXI_PHY=y
CONFIG_PHY_SUN4I_USB=y
CONFIG_NOP_USB_XCEIV=y

# Gadget framework + configfs-composed RNDIS (what x-chip-tools/init builds).
CONFIG_USB_GADGET=y
CONFIG_USB_LIBCOMPOSITE=y
CONFIG_USB_CONFIGFS=y
CONFIG_USB_CONFIGFS_RNDIS=y
CONFIG_USB_U_ETHER=y
